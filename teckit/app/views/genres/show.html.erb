<h1><%= @genre.name %></h1>

<h2>Movies:</h2>

<ol>
  <% @movies.each do |movie| %>
    <img src=<%= movie.thumbnail%> height="200" width="200">
    <h3><li> <%= link_to movie.name, movie_path(movie) %> </li></h3>
  <% end %>
</ol>
